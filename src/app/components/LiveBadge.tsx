import React from "react";

const LiveBadge = () => (
  <span className="absolute top-1 left-1 bg-blue-500 text-white text-[10px] font-bold px-1 border border-black">
    LIVE
  </span>
);

export default LiveBadge; 